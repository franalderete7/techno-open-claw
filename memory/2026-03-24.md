## Technifications OpenClaw - Web App Fix (2026-03-24)

### Problem
User complained that web app at 45.55.53.53:3000/ was showing all products automatically on homepage (looking horrible), product images were too large, and product names appeared duplicated. Wanted homepage to show only "TechnoStore" branding, with products visible only in the products tab in a proper table format.

### Solution
Fixed TypeScript/JSX build errors and created proper Next.js structure:

**Files created:**
- `apps/web/package.json` - Next.js 15.2.1 with React 19
- `apps/web/tsconfig.json` - Proper TS config with JSX preserve
- `apps/web/next.config.mjs` - Standalone output
- `apps/web/Dockerfile` - Multi-stage build (deps → build → runner)
- `apps/web/app/globals.css` - Base styles
- `apps/web/app/layout.tsx` - Navigation header with TechnoStore branding
- `apps/web/app/page.tsx` - Landing page (branding only, no products)
- `apps/web/app/products/page.tsx` - Data table with 24 columns
- `apps/web/app/api/products/route.ts` - API proxy with bearer token auth

**Key fixes:**
1. Homepage now shows only "TechnoStore 📱" branding + nav menu
2. Products display only on `/products` route in compact table format
3. API integration uses correct token: `aldegol` (not `aldeg`)
4. Table columns: SKU, brand, model, title, category, cost USD, price ARS, promo ARS, RAM, storage, network, stock, delivery, image link

### Deployment
- Built Docker image: `technifications-open-claw-web:latest`
- Container running on port 3000
- All services healthy: web, api, postgres, n8n

### Git
- Committed: "Fix web app: landing page + products table with proper API integration"
- Pushed to main (force): https://github.com/franalderete7/technifications-open-claw
- 22 files changed, 480 insertions, 50 deletions

### URLs
- Homepage: http://45.55.53.53:3000/
- Products: http://45.55.53.53:3000/products
- API: http://45.55.53.53:4000/v1/products
